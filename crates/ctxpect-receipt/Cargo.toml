[package]
name = "ctxpect-receipt"
description = "Formal Context Receipt construction, explicit snapshot migration, and local-continuity signatures."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ctxpect-schema = { path = "../ctxpect-schema" }

[lints]
workspace = true
