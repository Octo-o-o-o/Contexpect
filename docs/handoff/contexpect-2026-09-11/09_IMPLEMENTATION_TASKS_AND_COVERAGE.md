# 实施阶段、工程任务与全范围对账

**实施状态：任务尚未因本次归档被执行，也没有自动创建GitHub issue。** 本文保留最终建议的D0–D5、T01–T12和F-01–F-18映射，避免另建一套排期权威。[^R2]

## 1. 本轮交接后应先做什么

先核对实际工作区、当前commit、未提交修改与基线差异；确认现有有效PRD/ADR。然后处理模型与授权边界，再选T03/T04/T05/T06/T09的交集做一条纵向闭环。不要先扩展family名单、换掉全部基础设施，或根据本包图片重建整个项目。

用户已经确认白底黑字的视觉方向。实现UI时保留该方向；原产品功能、真值与权限仍按文本合同对齐。若找到本地HTML，先登记并复用；若没找到，不宣称完成了旧demo源码迁移。

## 2. 既有阶段安排
## 十、实施顺序、依赖与停止条件

### 10.1 按门槛推进，不虚构工期

以下阶段是对既有 WP 的实施组织，不新增 WP-13，也不修改原完成条件。没有团队规模、人员能力和当前分支差异的数据，因此不提供看似精确的周数、人日或完成率。每一阶段以产物与退出条件管理。

| 阶段 | 主要交付 | 前置依赖 | 退出条件 | 未达到时不得做什么 |
| --- | --- | --- | --- | --- |
| D0 基线与语义收口 | 实现/支持/执行证据矩阵；第 4 章修订项；来源与文档同步方案 | 固定代码与现有合同 | 观察范围、原生预期、策略、历史与当前状态不再混淆 | 不新增依赖来掩盖模型问题，不宣传完整支持 |
| D1 双锚点与证据纵向验证 | 已有 Codex/Claude 的真实静态输入、关键导入、最小真实故障集；至少对可取得的原生面补证 | D0 | 外部人员能复现；事实与 Unknown 清晰；至少一条真实证据桥连到 Receipt | 不以 synthetic、mapping 名称或 UI mock 冒充原生支持 |
| D2 Doctor 与用户路径收口 | 真实输入驱动诊断；APM 等只读产物导入；Doctor→证据→计划的可用路径 | D1 合同稳定部分 | 正常反例不会被错误自动修复；未知、失败和无命中分开；关键 E2E 可复跑 | 不启用外部工具直接 fix 或自动安装 |
| D3 基础设施有条件替换 | 必要依赖准入、离线材料、存储/文件/schema/API 单项迁移与回退 | D0；可与 D1/D2 非冲突部分并行 | 每项独立通过历史兼容、平台与故障测试 | 不同时切换全部基础设施，不长期双账本 |
| D4 管理、同步与团队信任 | 有限 native 投影、安全 apply/rollback、标准版本与例外、加密 bundle | D1/D2；需要的 D3 能力完成 | 冻结计划可恢复；post-Receipt；分叉不自动覆盖；身份边界真实 | 不用本地 MAC 冒充组织证明，不把传输当核验 |
| D5 全矩阵、分析与发行 | 扩展冻结适配范围；历史/Advisor/Effect；桌面与所有必需 OS、soak、人工及发行验收 | 相关共享合同稳定 | F/WP 所有必需项按原合同通过，独立 readback 完成 | 不把阶段试用版称为完整产品 |

**双锚点的“做深”不要求伪造两个工具都开放完整运行时接口。** 对能取得且合同要求的原生面真实验证；对确实未暴露的面保留 Unknown 并说明原因。适配矩阵中原本 required-supported 的能力不能因实现困难改成 required-unknown-honesty 来消除缺口。

### 10.2 可以并行与不应并行的工作

可并行：不同 adapter 的原生资料与独立样例、UI 无障碍测试、文档状态生成、外部产物合同设计、独立加密/签名准入验证。前提是共用对象与退出条件已经明确。

不应并行大改：核心 Claim/schema 与全部前端 DTO、存储引擎与签名字节、多个真实写入器、多个策略引擎、完整 UI 请求层和 API 路由重构。否则发生回归时难以定位，更容易让测试与实现同步漂移。

一个外部候选不合适时，应替换实现、缩小接入权限或保留只读输入；不能因此把它对应的必需产品能力永久删除。

### 10.3 立即停止扩张的信号

出现以下任一情况，应暂停相关新功能，先处理根因：旧 Receipt 无法验证；未授权读取或旁路落地正文；同一目标出现两个写入 authority；Unknown 被转换为通过；真实配置正常但被阻断为确认错误；无法确定某次修改到底写了哪些文件；同一查询在 CLI 与 UI 中得到不同事实。

这里的停止针对受影响的能力，不必让无关的只读检查和历史导出全部不可用。恢复要以负例转红、修复后再绿和独立复核为依据。

## 3. 既有工程任务单
## 十三、建议的工程任务单

这些是可转成 issue 的工作边界，不是本轮已经创建的 GitHub issue。每项应由实际团队指定负责人和独立复核者，避免把身份或排期凭空写死。

| 任务 | 优先级 | 目标与主要改动 | 前置 | 必交产物与通过条件 |
| --- | --- | --- | --- | --- |
| T01 冻结基线与状态对账 | P0 | 核对当前分支与本文基线；列出真实代码、执行证据、待实现项 | 无 | 一份与 CI/矩阵绑定的状态清单；不把旧文档状态当当前事实 |
| T02 修订真值与语义边界 | P0 | 处理第 4 章：观察/原生/策略、字节相同、声明来源、动作前置证据 | T01 | 规范变更说明、最小反例、schema/生成器/实现影响清单；独立 review |
| T03 做深原生 instructions 解析 | P0 | Codex/Claude 真实配置层、cwd、导入、预算来源和条件规则 | T02 | 对应版本样例与来源、正反例、原生对照和 Unknown 边界 |
| T04 建立真实原生证据桥 | P0/P1 | 至少一条可获得的原生输入→字段映射→Claim→Receipt；其余逐项推进 | T02/T03 相关合同 | 真实最小样例、损坏/缺段测试、幂等与删除；不使用 synthetic 冒充 |
| T05 Doctor 输入和规则收口 | P1 | 接通原生输入生产者；声明检查单列；高精度规则、suppression 和修复分级 | T03/T04 可用输入 | 独立正常反例、真实问题、规则解释；未执行与无命中分开 |
| T06 安全变更端到端 | P0/P1 | preview/授权/写入/恢复/rollback/post-Receipt 的共享应用服务 | T02；T03 可用于重验 | 跨入口一致；并发/崩溃/后续编辑反例；一次批准只执行冻结内容 |
| T07 依赖政策与单项基础设施升级 | P1，必要时阻断完整能力交付 | 提交 ADR 候选；按需要逐项引入 schema/文件/存储/加密等 | T01/T02 | 版本与 features、vendor、许可、兼容、迁移/回退测试；未批准不改依赖 |
| T08 外部报告与转换适配 | P1 | APM 只读产物、诊断报告；Rulesync 受限候选 | T02/T05；真实转换需执行授权 | 来源字段、退出码映射、I/O/网络边界；禁止直接写真实目录 |
| T09 UI/CLI 统一与桌面验证 | P1 | Doctor→证据→计划→结果闭环；坐标缓存；历史/当前视图；E2E | T03/T05/T06 已稳定部分 | 浏览器与桌面分开验收；测试插件从发行包排除 |
| T10 团队信任和加密同步 | P1/P2，完整交付必需 | 签名、版本谱系、例外、bundle、分叉、撤销与披露 | T06/T07 相关基础 | 双设备实际验证；本地连续性与组织信任分开；无明文旁路 |
| T11 历史、Advisor 与 Effect | P2，完整交付必需 | 最小发送、派生失效、真实 runner、独立统计与日志准入 | T04/T07 相关基础 | 授权记录、原始日志边界、冻结实验及适用性；未执行如实显示 |
| T12 全矩阵与发行收口 | P1/P2，持续准备 | 所有冻结 required cell、OS、恢复、soak、人工、安装升级与独立 readback | 相关 T03–T11 | 原 WP-12 完整记录、可核验发布包、准确 release notes |

### 13.1 建议下一批实际工作

优先完成 T01/T02，然后以 T03/T04/T05/T06/T09 的交集做一条真实纵向闭环。T07 可并行准备决策与离线材料，但不要先进行大范围换库。T08 先接只读产物，避免过早承担外部执行副作用。

如果必须在“再加一个 adapter 名称”和“让现有一个真实问题从诊断到修复可独立复现”之间选择，优先后者。完成该闭环后，把已稳定的测试与接口复制到其余冻结 family，而不是重新发明流程。

### 13.2 每个任务完成时必须交付的说明

简要说明改了什么、没有改什么、对应哪个 Feature/WP、输入输出合同是否变化、来源版本、实际执行的测试、未执行项、已知风险与回退方法。不能只提交“完成”“全部绿色”或大段解释而没有证据。

代码审查优先查看安全边界与反例，再看界面截图和功能数量。实现者与复核者可以协作，但对关键结论应保存独立判断过程所使用的输入和结果，不把同一次交付中的角色切换包装成完整独立验收。

## 4. T09 的设计/演示子任务

这些是对已有T09的交接细化，不新增产品Feature或工作包；建议编号供任务拆解，不代表已经获批或已创建issue。

| 子任务 | 内容 | 交付证据 |
| --- | --- | --- |
| T09.U01 资产对账 | 找到现有HTML/React、UI图和状态合同；记录哈希/commit | 实际可访问源码列表，找不到的项明确标记 |
| T09.U02 黑白浅色统一 | 现有token映射到白底黑字；移除大面积深色/霓虹默认 | 主要路由真实截图，长文本/焦点/灰度可读 |
| T09.U03 完整入口保留 | V01–V16及F13/F14真实可达，9屏只作体验叙事 | 路由表与覆盖测试，无空壳删功能 |
| T09.U04 可交互演示 | 稳定fixture、状态切换、预览/冲突/回滚、导出/重置 | DEMO-01–18适用场景的实际结果，不是只有截图 |
| T09.U05 真实API对接 | 数据源边界、坐标generation、错误/Unknown、历史视图 | CLI/API/UI在同输入下的事实一致测试 |
| T09.U06 平台与发行隔离 | 浏览器/真实WebView分别验；测试驱动不出货 | 各lane结果与最终构建的测试能力排除检查 |

## 5. 原完整范围映射
### 11.3 原完整范围的覆盖回查

此表只检查最终建议有没有遗漏既有 Feature，不替代仓库原 traceability。

| Feature | 必须完成的内容 | 本文落点 |
| --- | --- | --- |
| F-01 | 环境、family/version/surface 与兼容能力 | M01/M02、9.1、D1/D5 |
| F-02 | 资产发现、安全读取与不可读原因 | M01、4.1、Q01/Q02 |
| F-03 | 原生静态解析、Claim 与证据边界 | M02/M03、4.1–4.4、9.1 |
| F-04 | preflight、启动/会话关联和相应入口 | 闭环 A/B、9.2、D1/D2 |
| F-05 | Receipt、来源、签名、导出与删除 | M05、4.5/4.8、9.5、Q08/Q16 |
| F-06 | 可用的桌面检查与全路由交互 | M15、闭环 A–E、9.8、D5 |
| F-07 | 跨坐标差异与等价比较 | M07、4.2、9.5、Q03/Q05 |
| F-08 | Doctor、真实规则、安全建议与输出 | M08、4.4/4.7、9.3、D2 |
| F-09 | CanonicalIntent、投影、apply/rollback、核对 | M10/M11、5.3、9.4、D4 |
| F-10 | 加密多设备同步与相关标准采用 | M12、闭环 E、9.7、D4 |
| F-11 | 资产生态、来源、许可与 SBOM | M09/M20、6、7、D2/D5 |
| F-12 | 真实 runtime importer 与会话 | M04、9.2、闭环 B、D1/D5 |
| F-13 | LLM Advisor 及发送授权 | M16、9.9、D5 |
| F-14 | 历史洞察、观察窗口及失效传播 | 4.5/4.8、9.5/9.9、D5 |
| F-15 | 冻结实验、runner、统计与限制 | M17、9.10、Q15、D5 |
| F-16 | daemon、调度、CLI/CI、资源与发行 | M14/M18/M20、9.8、D2/D5 |
| F-17 | adapter 合同、SDK、schema 与扩展验收 | M02/M03/M19、9.1、D5 |
| F-18 | 团队策略、例外、身份与执行性 | M13、4.3、9.6、闭环 E、D4 |

原生执行、依赖安装、组织身份部署和付费实验各自需要适当权限与验证。范围保留不等于一次性授权执行所有动作。[^plan]

## 6. 原声明的18个adapter family

本会话仓库基线声明包括Codex、Claude Code、Cursor、Grok Build；OpenCode、Kimi Code、ZCode、Qwen Code、Goose、Gemini CLI、GitHub Copilot CLI、Kiro；DeepSeek Harness、Cline、Aider、OpenHands、Windsurf、Coze。该列表是冻结声明范围，不是已全部支持证明。每个family要继续按version/surface/capability/适用环境细分并验收。[^families]

Coze的connector/executor不能强套隐藏prompt等价；Aider不能强套SKILL.md模型。某工具未提供所需原生面时保留正确边界，但原本required-supported的实现缺口不能用改标签消除。

## 7. 任务完成后的统一回执

每项说明：改了什么、没改什么、F/WP映射、合同是否变化、来源版本、实际执行测试、未执行项、风险、回退方法和证据位置。不要只交一段“全部完成/全绿”。对政策或golden修订，单列旧语义、新证据和复核者，而不回改历史记录使其看似从未出错。


## 本文依据

[^R2]: 本会话《最终建议与实施决策书》，[原文](originals/Contexpect_Final_Recommendations_2026-09-11.md)。这是最终咨询取舍，不是仓库已接受的新 ADR，也不是已完成的实现。

[^plan]: Contexpect，[实施计划](https://github.com/Octo-o-o-o/Contexpect/blob/257b7d4d82c0865a94b45d9b3fddcb3830658b32/docs/process/implementation-plan.md)。用于冻结 cutoff、F-01–F-18 / WP-01–WP-12 映射及完整交付条件；基线提交入口为 [257b7d4](https://github.com/Octo-o-o-o/Contexpect/commit/257b7d4d82c0865a94b45d9b3fddcb3830658b32)。

[^families]: 本会话读取的 Contexpect [README 固定基线](https://github.com/Octo-o-o-o/Contexpect/blob/257b7d4d82c0865a94b45d9b3fddcb3830658b32/README.md) 与实施计划。家族清单是声明范围；本轮未重新执行安装/适配探测。
